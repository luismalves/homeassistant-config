sd1: >
  [[[
    return `<span>Com base em faturas</span>`
  ]]]

info1: >
  [[[
    let state = (states['sensor.current_month_kwh_charged'].state == 'unavailable' || states['sensor.current_month_kwh_charged'].state == 'unknown') ? '-' : states['sensor.current_month_kwh_charged'].state;
    return `<span>${state}</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">kWh (C)</span>`
  ]]]

info2: >
  [[[
    let state = (states['sensor.current_month_ev_charging_cost'].state == 'unavailable' || states['sensor.current_month_ev_charging_cost'].state == 'unknown') ? '-' : states['sensor.current_month_ev_charging_cost'].state;
    return `<span>${state}</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">EUR/kWh (C)</span>`
  ]]]

info3: >
  [[[
    let state = (states['sensor.id3_odometer'].state == 'unavailable' || states['sensor.id3_odometer'].state == 'unknown') ? '-' : states['sensor.id3_odometer'].state;
    return `<span>${state}</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">Total KMs</span>`
  ]]]

info4: >
  [[[
    let state = (states['sensor.previous_month_ev_charging_cost'].state == 'unavailable' || states['sensor.previous_month_ev_charging_cost'].state == 'unknown') ? '-' : states['sensor.previous_month_ev_charging_cost'].state;
    return `<span>${state}</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">EUR/kWh (P)</span>`
  ]]]
