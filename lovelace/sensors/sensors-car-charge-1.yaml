sd1: >
  [[[
    return `<span>Atualizado às ${new Date(states['sensor.my_id_charge_power'].last_changed).toTimeString().slice(0, 5)}</span>`
  ]]]

info1: >
  [[[
    let state = (states['sensor.my_id_charge_power'].state == 0) ? '-' : states['sensor.my_id_charge_power'].state ;
    return `<span>${state}</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">kW</span>`
  ]]]

info2: >
  [[[
    let state = (states['sensor.my_id_charge_rate'].state == 0) ? '-' : states['sensor.my_id_charge_rate'].state ;
    return `<span>${state}</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">km/h</span>`
  ]]]

info3: >
  [[[
    return `<span>${states['sensor.my_id_state_of_charge'].state}%</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">Current</span>`
  ]]]

info4: >
  [[[
    return `<span>${states['number.my_id_target_state_of_charge'].state}%</span><span style="color:var(--el-text-color-light);font-size:10px;font-weight:400;">Target</span>`
  ]]]
