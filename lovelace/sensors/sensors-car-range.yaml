number: >
  [[[
    if(entity.state == 'unavailable') { return '<span>?</span>' } 
    else { return '<span>' + entity.state + '</span>' }
  ]]]

label: >
  [[[
    return `<span>KM restantes</span>`
  ]]]
