|
  :host {
    flex: 2 !important;
  }
  ha-card {
    background: var(--ha-card-background-es-off);
    border-radius: 15px;
    box-shadow: none;
  }
  .states {
    color: var(--ha-card-color-name-on);
    padding-bottom: 8px;
    padding-left: 8px;
  }
