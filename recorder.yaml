############################################################
# Recorder
############################################################

db_url: !secret recorder_db_url

auto_purge: true
purge_keep_days: 10

exclude:
  domains:
    - update
    - automation
    - group
    - sun
    - weather
  entity_globs:
    - sensor.*uptime*
    - sensor.*battery_level
